import RequestApprovalContainer from "../components/ui/containers/RequestApprovalContainer";

const RequestApprovalPage = () => {
  return <RequestApprovalContainer />;
};

export default RequestApprovalPage;
